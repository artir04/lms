[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --strict-markers --tb=short
markers =
    unit: pure-Python unit tests, no I/O dependencies
    integration: tests that exercise the FastAPI app + database

filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::DeprecationWarning:bcrypt.*
